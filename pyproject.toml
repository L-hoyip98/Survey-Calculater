[tool.poetry]
name = "surveyor-calculater"
version = "0.1.0"
description = ""
authors = ["HoYipYip1 <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
tabulate = "^0.8.9"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
